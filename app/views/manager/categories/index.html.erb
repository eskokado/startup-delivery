<section class="bg-gray-50 dark:bg-gray-900 p-3 sm:p-5" data-controller="categories--index">
  <div class="mx-auto max-w-screen-xl px-4 lg:px-12">
    <div class="bg-white dark:bg-gray-800 relative shadow-md sm:rounded-lg">
      <div class="overflow-x-auto">
        <table class="w-full text-sm text-left text-gray-500 dark:text-gray-400">
          <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
          <tr>
            <th scope="col" class="px-4 py-3"><%= Category.human_attribute_name("name") %></th>
            <th scope="col" class="px-4 py-3"><%= Category.human_attribute_name("description") %></th>
            <th scope="col" class="px-4 py-3"><%= Category.human_attribute_name("image_url") %></th>
            <th scope="col" class="px-4 py-3"></th>
          </tr>
          </thead>
          <tbody>
          <%= render @categories %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</section>
